{
  "name": "backend",
  "version": "0.0.1",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:ui": "rm -rf dist && cd ../../part2/rendering-collections && npm run build && cp -r dist ../../part3/notes-backend"
  },
  "author": "Carlos Hubert",
  "license": "MIT",
  "description": "",
  "dependencies": {
    "express": "^5.1.0"
  }
}
